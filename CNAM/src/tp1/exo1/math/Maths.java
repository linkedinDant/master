package tp1.exo1.math;

public class Maths {

  static public int fact(int n) {
    // Factorielle récursive
    return 0;
  }

  static public int factIteratif(int n) {
    // Factorielle itérative (avec une boucle for et sans que la fonction ne s'appelle elle-même, comme dans la fonction récursive)
    return 0;
  }
  
  /**
   * Cette fonction renvoie le nombre "n" à la puissance "d". Cela implique qu'il faut multiplier n d fois. 
   * Par exemple pow(3, 4) renverra : 3 * 3 * 3 * 3 -> 81. On a bien multiplié d fois (4 ici) n (3 ici)
   */
  static public int pow(int n, int d) {
    return 0;
  }
  
  static public int powIteratif(int n, int d) {
    // Version itérative de la puissance
    return 0;
  }

  /**
   * Renvoie la valeur absolue du nombre entier spécifié. La valeur absolue de x, notée |x|, est la valeur numérique de x, sans tenir compte de son signe.
   * Ainsi si 
   *  x = 5, |x| = 5
   *  x = -5, |x| = 5 
   * @param val le nombre dont on veut la valeur absolue 
   * @return la valeur absolue du paramètre
   */
  static public int abs(int val) {
    return 0;
  }

  static public int min(int a, int b) {
    // Renvoie le nombre le plus petit entre a et b
    return 0;
  }

  static public int max(int a, int b) {
    // Renvoie le nombre le plus grand entre a et b
    return 0;
  }
}
